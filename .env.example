# Firebase Config (Managed via firebase-applet-config.json)
# Supabase (Legacy)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# OTP Bridge Configuration
# A secret key to authenticate your mobile forwarding app (e.g. Tasker/Macrodroid)
OTP_BRIDGE_SECRET=
